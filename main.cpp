#include <iostream>
#include <cstring>

using namespace std;

const int MOD = 10000000;
int cache[101][101];

int main() {
	int C;
	cin >> C;
	while (C--) {
		int n;
		cin >> n;
		memset(cache, -1, sizeof(cache));
	}
	return 0;
}