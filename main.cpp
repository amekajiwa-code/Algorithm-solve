#include <iostream>
#include <cstring>
#include <algorithm>

using namespace std;

int cache[101];

int main() {
	int C;
	cin >> C;
	while (C--) {
		int width;
		cin >> width;
		memset(cache, -1, sizeof(cache));
	}
	return 0;
}